# Tailscale Configuration
# Get auth key from: https://login.tailscale.com/admin/settings/keys

# Required: Auth key (reusable recommended for servers)
TAILSCALE_AUTHKEY=tskey-auth-xxxxx

# Hostname shown in Tailscale admin console
TAILSCALE_HOSTNAME=vps-server

# Use userspace networking (no kernel module needed)
TS_USERSPACE=true

# Subnet routes to advertise (optional)
# Example: 172.18.0.0/16 for Docker network
TS_ROUTES=

# Extra arguments for tailscale up
# Example: --advertise-exit-node --accept-routes
TS_EXTRA_ARGS=
